# Changelog

记录回测系统的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [v1.1] - 2026-08-27

### 新增
- **默认 500 只随机样本**：`evaluate_engine` 的 `n_sample` 默认改为 500（之前 100）
- **缓存 7 天自动更新**：`load_kline` 加 `max_age_days=7`，缓存文件超 7 天自动重拉，数据不过期
- **`long_only` 模式进 CLI**：`run_backtest --exit-mode` 增加 `long_only`（只买不卖）选项

### 修复
- **CLI `--exit-mode` 缺 `long_only`**：底层已支持只买不卖，但 CLI choices 只有 signal/trailing，已补全

### 优化
- **缓存过期机制**：解决数据永不过期、用旧行情测新引擎的失真问题

## [v1.0] - 2026-08-27

### 新增
- **内置参考引擎** `src/engines.py`：三个经典波段引擎（双均线 `ma_cross` / MACD `macd_cross` / RSI 超买超卖 `rsi_reversal`），作为你开发新引擎的对比基准
- **标准引擎评估接口** `src/benchmark.py:evaluate_engine()`：测引擎的标准入口——自动拉数据/归一化列序/对齐信号长度/选出场模式/对比3个经典引擎，一次跑出完整对比，避免手拼数据出错
- **双向信号支持**：买入 `sig>=th`、卖出 `sig<=-th`（金叉买/死叉卖），波段引擎能主动平仓
- **`exit_mode` 出场模式**：`signal`（默认，有买有卖）/ `long_only`（可选，只买不卖）/ `trailing`（趋势引擎吊灯ATR+BE+TP止损止盈），区分引擎类型、公平测量
- **`reference_exit_mode`**：`evaluate_engine` 里 3 个经典对照引擎的出场模式（默认 signal），可与被测引擎同模式（如都只买不卖）对比
- **回归测试** `tests/test_backtest.py`：性质验证（无信号净值恒1、全仓净值手工吻合、ATR 无未来函数、跳空大跌止损生效、sig 长短健壮性等）
- **19 项绩效**：接入 easy-tdx `PerformanceAnalyzer`（收益/年化/回撤/夏普/索提诺/卡玛/利润因子/波动率/胜率等）

### 修复
- **ATR 未来函数（重大）**：原 `np.convolve(mode="same")` 中心对齐，第 i 天止损决策时偷看未来 6 天数据，导致回测绩效虚高。改为 **cumsum 纯历史滚动**，彻底消除未来暴露。
- **sig 数组越界崩溃**：`single_daily_rets` 直接 `sig[i]` 访问，若传入信号比 df 短会 IndexError。改为函数开头归一化 sig 到与 df 等长（短→补0，长→截断）。
- **期末平仓双重计收益**：持仓收益已逐日计入净值，期末又乘一遍导致虚高。改为只扣卖出成本。
- **止损跳空保守成交**：跳空低开时按更差的开盘价成交（参考 easy-tdx），防「锁在止损价」失真。
- **next_open 建仓**：信号次日开盘成交，不再用信号日收盘价（消除未来函数）。
- **数据层板块感知阈值**：复权检测 `ex_div_chg`/自检阈值改按板块涨跌停幅度（主板10%/双创20%/北交所30%），修复「固定13% 误拦创业板/科创板/北交所导致样本偏向主板」的 bug。
- **walkforward `__main__` 导入**：非相对导入导致失败，改 `.data_source`。

### 优化
- **compute_atr 向量化**：逐日 Python 循环 → cumsum 前缀向量化（477只×2500根下提速明显）
- **walkforward 预计算日收益**：避免 7 窗重复跑完整资金曲线（提速，结果不变）
- **evaluate_engine 去硬编码**：删除 `_th = 15 if name == "你的A股引擎"` 特殊分支，阈值统一由参数控制（引擎对比之前靠手拼易出错，现已标准化）

### 定位
- 本项目为**回测框架**，内置 3 个参考引擎（对比基准）+ demo 信号（验证链路），用于测试指标引擎（波段策略）。
- **无未来函数 + 严格样本外 WF** 是硬地基：能可靠评估引擎的真实性能。
- `exit_mode` 区分波段/趋势引擎，避免用错出场逻辑导致误测（曾把波段引擎叠趋势止损止盈，误测成全亏；改用 `signal` 后转正：MACD +437% / RSI +225% / 双均线 +199%）。

### 已知
- A股引擎（gate_trmo）经 ATR 修复后实际泛化能力有限（7窗WF 约 +1%），说明原有策略结论受未来函数污染，需重新审视。
- 原脚本 `/tmp/bt` 的矩阵引擎同样带 ATR 未来函数，其历史结论（如 +411%/+1553%）应打折扣。
