# Changelog

记录回测系统的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

> 版本划分：
> - **v1.0** = 核心回测引擎（无未来函数/真实费率/自研前复权/样本外WF/绩效）
> - **v1.1** = 从「制定引擎接口」起的所有改进（接口/参考引擎/出场模式/样本配置/数据保鲜/去硬编码）

## [v1.1] - 2026-08-27

### 新增
- **标准引擎评估接口** `src/benchmark.py:evaluate_engine()`：测引擎的标准入口——自动拉数据/归一化列序/对齐信号长度/选出场模式/对比3个经典引擎一次跑完，避免手拼数据出错
- **内置参考引擎** `src/engines.py`：双均线 `ma_cross` / MACD `macd_cross` / RSI 超买超卖 `rsi_reversal`，作为你开发新引擎的对比基准
- **双向信号支持**：买入 `sig>=th`、卖出 `sig<=-th`（金叉买/死叉卖），波段引擎能主动平仓
- **`exit_mode` 出场模式**：`signal`（默认，有买有卖）/ `long_only`（可选，只买不卖）/ `trailing`（趋势引擎吊灯ATR+BE+TP止损止盈），区分引擎类型、公平测量
- **`reference_exit_mode`**：`evaluate_engine` 里 3 个经典对照引擎的出场模式（默认 signal），可与被测引擎同模式（如都只买不卖）对比
- **默认 500 只随机样本**：`evaluate_engine` 的 `n_sample` 默认改为 500（之前用 100）
- **缓存 7 天自动更新**：`load_kline` 加 `max_age_days=7`，缓存文件超 7 天自动重拉，数据不过期
- **`long_only` 模式进 CLI**：`run_backtest --exit-mode` 增加 `long_only`（只买不卖）选项

### 修复
- **ATR 未来函数（重大，v1.0 遗留已在此彻底修净）**：原 `np.convolve(mode="same")` 中心对齐，第 i 天止损决策时偷看未来 6 天数据，导致回测绩效虚高。改为 **cumsum 纯历史滚动**。
- **sig 数组越界崩溃**：`single_daily_rets` 直接 `sig[i]` 访问，若传入信号比 df 短会 IndexError。改为函数开头归一化 sig 到与 df 等长（短→补0，长→截断）。
- **CLI `--exit-mode` 缺 `long_only`**：底层已支持只买不卖，但 CLI choices 只有 signal/trailing，已补全
- **evaluate_engine 去硬编码**：删除 `_th = 15 if name == "你的A股引擎"` 特殊分支，阈值统一由参数控制，引擎对比不再靠手拼

### 优化
- **compute_atr 向量化**：逐日 Python 循环 → cumsum 前缀向量化（477只×2500根下提速明显）
- **walkforward 预计算日收益**：避免 7 窗重复跑完整资金曲线（提速，结果不变）
- **缓存过期机制**：解决数据永不过期、用旧行情测新引擎的失真问题

### 定位
- 本项目为**回测框架**，内置 3 个参考引擎（对比基准）+ demo 信号（验证链路）+ 标准接口 `evaluate_engine`，用于测试你开发的指标引擎（波段策略）。
- **无未来函数 + 严格样本外 WF** 是硬地基：能可靠评估引擎的真实性能。
- `exit_mode` 区分波段/趋势/只买不卖引擎，避免用错出场逻辑导致误测。

## [v1.0] - 2026-08-27

### 核心引擎（回测系统本身）
- **无未来函数**：ATR 用 cumsum 纯历史滚动；成交 next_open 次日开盘（信号日收盘价成交属未来函数，已消除）
- **真实 cost 模型**（v1.1 完善）：佣金双边 + 印花税卖出 + 滑点；期末平仓只扣卖出成本；止损跳空按更差开盘价
- **自研前复权数据层**：easy-tdx 拉 NONE 原始价 + 向下跳空检测前复权（板块感知阈值），消除除权假跳变；本地缓存
- **严格 7 窗样本外 WF**：后70%切7段，看引擎真实泛化
- **多标的等权组合**：资金池流动，绩效用 easy-tdx PerformanceAnalyzer 出 19 项

### 已修（随 v1.0 落地）
- 期末平仓双重计收益：持仓收益已逐日计入，期末又乘一遍导致虚高 → 只扣卖出成本
- 止损跳空保守成交：跳空低开按更差开盘价
- 数据层板块感知阈值：主板10%/双创20%/北交所30%，修复固定13%误拦双创/北交所
- walkforward `__main__` 导入：非相对导入导致失败，改 `.data_source`

### 已知
- A股引擎（gate_trmo）经 ATR 修复后实际泛化能力有限（7窗WF 约 +1%），说明原有策略结论受未来函数污染，需重新审视。
- 原脚本 `/tmp/bt` 的矩阵引擎同样带 ATR 未来函数，其历史结论（如 +411%/+1553%）应打折扣。
